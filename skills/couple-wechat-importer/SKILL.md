---
name: couple-wechat-importer
description: 情侣复盘系统 · 微信聊天记录读取器。连接已解密的微信数据库，提取特定联系人的完整聊天记录，格式化输出时间线并标记情绪转折点与依恋行为，为后续复盘提供结构化素材。内置微信解密工具，无需外部依赖。
category: couple-retrospective
---

# 微信聊天记录读取器

## 作用

从已解密的微信数据库中提取**特定联系人**的聊天记录，输出：
1. 标准化时间线（格式化对话）
2. 行为分析数据（发言比例、回复延迟、情绪转折、沉默窗口）
3. 依恋行为标记（焦虑型/回避型信号自动识别）
4. 结构化 JSON + Markdown 报告

输出直接供 `couple-retrospective` 主框架作为 **Step 0 素材** 使用。

---

## 内置工具

本 skill 内置完整的微信解密工具，无需外部依赖：

```
tools/
├── wechat_decryptor.py   # 解密主脚本（从内存提取key + 解密数据库）
├── wechat_parser.py      # 聊天记录解析脚本
└── wxManager/            # 核心解密库
    └── decrypt/
        ├── get_wx_info.py      # key 提取
        ├── decrypt_v3.py        # 数据库解密
        └── version_list.json    # 版本列表
```

---

## 快速开始（Step by Step）

### Step 1：提取 Key + 解密数据库

```python
import sys, os, json

# 切换到工具目录
TOOLS_DIR = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, TOOLS_DIR)
os.chdir(TOOLS_DIR)

from wxManager.decrypt.get_wx_info import read_info
with open('./wxManager/decrypt/version_list.json', encoding='utf-8') as f:
    vl = json.load(f)

result = read_info(vl)
key = result[0]['key']
wxid = result[0]['wxid']
print(f'Key: {key}, wxid: {wxid}')

# 解密数据库
from wxManager.decrypt.decrypt_v3 import decrypt_db_files
src = fr'D:\\Documents\\WeChat Files\\{wxid}\\Msg'
out = r'D:\\Claude_Notes\\wechat_decrypted'
os.makedirs(out, exist_ok=True)
decrypt_db_files(key, src, out)
print('解密完成！')
```

**成功标志：** `errcode=200`，输出 `MicroMsg.db` 和 `Multi/MSG0.db`

### Step 2：提取联系人

```bash
python tools/wechat_parser.py --db-dir ./decrypted/ --list-contacts
```

### Step 3：提取聊天记录

```bash
python tools/wechat_parser.py --db-dir ./decrypted/ --target "方B" --no-context
```

---

## 前置条件

1. **微信正在运行**（Key 从内存提取，需微信登录状态）
2. **每次微信重启后需重新提取 Key**
3. 已解密数据库（可用内置工具）

---

## 数据库结构

### MicroMsg.db · Contact 表

| 字段 | 说明 |
|------|------|
| `UserName` | wxid（如 `wxid_xxxxx`） |
| `Alias` | 微信号（自定义英文ID） |
| `Remark` | 备注名 |
| `NickName` | 微信昵称 |

### MSG0.db · MSG 表

| 字段 | 说明 |
|------|------|
| `CreateTime` | Unix 时间戳（秒级） |
| `Type` | 消息类型（1=文本，3=图片，34=语音，43=视频，10000=系统消息） |
| `IsSender` | 1=我发送的，0=对方发送的 |
| `StrContent` | 消息文本内容 |
| `TalkerId` | 关联 Name2ID 表的 _id |

### 消息类型编码

| Type | 类型 |
|------|------|
| 1 | 文本 |
| 3 | 图片 |
| 34 | 语音 |
| 43 | 视频 |
| 47 | 表情 |
| 49 | 链接/XML/小程序 |
| 10000 | 系统消息（撤回提示等） |

---

## 执行流程

### Step 0：确认数据源

用户提供：
- 解密数据库路径（默认 `D:\Claude_Notes\wechat_decrypted_v2\`）
- 联系人昵称或备注名

### Step 1：查找 wxid

```sql
SELECT UserName, Remark, NickName 
FROM Contact 
WHERE NickName LIKE '%目标昵称%' OR Remark LIKE '%目标昵称%';
```

### Step 2：提取聊天记录

```sql
SELECT
    m.CreateTime,
    m.IsSender,
    m.Type,
    m.StrContent
FROM MSG m
LEFT JOIN Name2ID n ON n._id = m.TalkerId
WHERE n.UsrName = 'wxid_xxx'
  AND m.Type = 1
ORDER BY m.CreateTime ASC;
```

### Step 3：格式化时间线

```
[YYYY-MM-DD HH:MM] [发送方] 内容
```

### Step 4：行为分析（自动）

#### 4.1 时间维度

| 指标 | 计算方法 | 复盘价值 |
|------|---------|---------|
| 消息总量 | 总条数 | 亲密度基准线 |
| 日均消息量 | 总数 / 天数 | 日常连接频率 |
| 峰值日期 | 单日最多消息数 | 重要事件锚点 |
| 深夜占比 | 00:00-05:00 消息 / 总消息 | 生活节奏匹配度 |
| 沉默窗口 | 连续 >4h 无消息 | 回避行为信号 |

沉默窗口严重度：>24h = 严重，>48h = 危险

#### 4.2 情绪维度

**情绪关键词分层统计：**

| 层级 | 关键词示例 | 统计方式 |
|------|-----------|---------|
| 正向 | 喜欢、爱你、想你、哈哈 | 按发送方计数 |
| 负向 | 算了、不想、别烦、滚 | 按发送方计数，附上下文 |
| 追逐信号 | 你在干嘛、怎么不回、睡了吗 | 焦虑型行为标记 |
| 回避信号 | 在忙、晚点、先不聊、困了 | 回避型行为标记 |

**情绪转折点识别：**
- 连续3条消息情绪标签翻转（正→负 或 负→正）
- 沉默窗口后第一条消息带强烈情绪词
- 某关键词在10分钟内出现2次以上

#### 4.3 沟通模式维度

| 指标 | 说明 |
|------|------|
| 发言比例 | 双方各发了多少条，差距多大 |
| 回复延迟分布 | 平均多久回、中位数、即时回复率 |
| 典型开场/收尾句式 | 每人常用的第一句话和最后一句话 |
| 单字敷衍频率 | "嗯""哦""行""好"的计数 |

**关键洞察示例：**
- 方A平均回复 42 分钟，方B 8 分钟 → 感知差距 5 倍
- 方A深夜消息多 → 情绪在夜间暴露
- 方B单字敷衍 45 次 → 回避型信号

#### 4.4 轮次分析（核心）

将连续对话按**轮次**切分（A 发言 → B 回应 = 1 轮），逐轮评估：

| 轮次评估维度 | 说明 |
|-------------|------|
| 情绪走向 | 本轮情绪是升温 / 持平 / 降温 |
| 回应质量 | 是否接住了对方的情绪/事实 |
| 雷点触发 | 本轮是否踩雷（说了不该说的话） |
| 修复行为 | 本轮是否有道歉、解释、示弱等修复动作 |
| 话题层级 | 事实层 → 情绪层 → 需求层，是否对齐 |

**轮次级输出示例：**
```
第 7 轮 | 2026-04-06 03:12
方A："不能闲，停下来闭眼就是你"
方B："要脸干嘛要你就行了"
情绪走向：↑ 升温（试探 → 直球接纳）
回应质量：高（方A暴露脆弱，方B直接接住）
雷点：无
话题层级：情绪层 → 情绪层（对齐）
```

**关键洞察：** 复盘不是看"发了多少条"，而是看"第几轮崩掉的、为什么崩、哪一轮本可以救回来"。

#### 4.5 依恋行为标记

**焦虑型信号（自动检测）：**
- 同一问题被重复问 2 次以上
- "你会走吗""你还在吗""真的吗"等确认句式
- 主动求助频率（"帮我""怎么办"）

**回避型信号（自动检测）：**
- 超时回复（>2h）次数
- 话题回避（从感情话题切到工作/游戏/睡觉）
- 单字回复占比

**追逃循环检测：**
- 追逐方试探 → 回避方沉默/敷衍 → 追逐方爆发/放弃
- 统计循环次数、典型触发话题

### Step 5：论文级分析（核心）

基于 Paper A/B/C 理论框架，对聊天记录进行深度解读，输出带论文框架的分析结论。

#### Paper A：追逐-逃避循环

识别对话中是否出现追逐-逃避循环：

| 检查项 | 判定标准 |
|--------|---------|
| 循环启动轮次 | 哪一轮出现了"追逐方发出信号 → 回避方沉默/敷衍" |
| 循环加速节点 | 哪一轮追逐方开始升级信号（质问/情绪爆发） |
| 循环断裂节点 | 哪一轮对话彻底崩掉或意外修复 |
| 双方角色 | 谁是追逐方、谁是回避方（可能互换） |

**输出示例：**
```
追逐-逃避循环检测：✅ 发现 2 次循环

循环 #1 | 第 12-18 轮
- 启动：第 12 轮，方A问"你在干嘛"（试探信号）
- 加速：第 15 轮，方A连发 5 条消息（信号升级）
- 断裂：第 18 轮，方B说"算了"（逃避方关闭对话）
- 角色：方A=追逐方，方B=回避方

循环 #2 | 第 31-35 轮
- 启动：第 31 轮，方B问"你是不是不在乎了"
- ...（方A变为回避方，方B变为追逐方，角色互换）
```

#### Paper B：结构性冲突

判断冲突性质是**沟通问题**还是**系统设计冲突**：

| 维度 | 沟通问题 | 结构性冲突 |
|------|---------|-----------|
| 双方需求 | 可以被同时满足 | 本质上互相排斥 |
| 解决方式 | 换个说法就行 | 需要系统级妥协 |
| 典型表现 | "你语气不好" | "我要空间，你要亲密" |

**输出示例：**
```
结构性冲突诊断：✅ 结构性冲突

方A的核心需求：自主/空间/不被追问
方B的核心需求：被回应/被记住/被确认

结论：这不是"谁不会说话"，是两个操作系统在报错。
改进方向不是"方A多说点"，而是建立"最低确认信号"机制。
```

#### Paper C：EWDM 期望-撤退动态模型

追踪对话中期望（E）和撤退（W）的变化曲线：

```
E（期望）↑ → 追逐行为 → W（撤退）↑ → 循环加速
                ↓
        打破循环的节点 = 某一方停止追逐/撤退
```

**输出示例：**
```
EWDM 曲线分析：

第 1-5 轮：E 平稳，双方正常交流
第 6 轮：方B 30 分钟未回 → E(方A) ↑（期望上升）
第 8 轮：方A追问"人呢" → 追逐行为启动
第 10 轮：方B回复"在忙" → W(方A) ↑（失望/撤退感）
第 12 轮：方A情绪爆发 → E(方B) ↑（对方A的反应产生压力期望）

打破循环的潜在节点：
- 第 10 轮，如果方B说"在忙，晚点聊"而非"在忙"，E(方A) 不会继续上升
- 第 12 轮，如果方A停止追问，给方B空间，循环在此断裂
```

#### 论文级输出汇总

```
## 📄 论文分析摘要

### Paper A：追逐-逃避循环
- 循环次数：N 次
- 主要角色：方A=追逐方，方B=回避方（第 X 轮角色互换）
- 关键循环：第 N 轮启动 → 第 N 轮加速 → 第 N 轮断裂

### Paper B：结构性冲突
- 冲突性质：沟通问题 / 结构性冲突
- 方A 核心需求：...
- 方B 核心需求：...
- 双方需求是否兼容：是 / 否

### Paper C：EWDM 动态模型
- 期望上升节点：第 N 轮（触发事件：...）
- 撤退加速节点：第 N 轮（触发事件：...）
- 打破循环的最佳时机：第 N 轮（如果...）

## 💡 基于论文的改进建议（最多3条）
1. ...（对应 Paper X）
2. ...（对应 Paper X）
3. ...（对应 Paper X）
```

---

## 输出格式

### Markdown 报告模板

```markdown
# 聊天记录分析报告 — {方A} × {方B}

## 📊 基本概览
| 指标 | 数值 |
|-----|------|
| 时间范围 | YYYY-MM-DD ~ YYYY-MM-DD |
| 总消息数 | N 条 |
| 日均 | N 条 |
| 峰值日 | MM-DD（N条） |
| 深夜占比 | N% |

## 🗣️ 发言比例
方A N% ████████████████████░░░░ N条
方B N% █████████████░░░░░░░░░░░░ N条

## ⏱️ 回复速度
| 指标 | 方A | 方B |
|-----|------|------|
| 平均延迟 | N分钟 | N分钟 |
| 即时回复率 | N% | N% |

## 😢 情绪关键词
### 负面情绪
- 方A："算了"(N次) "不想"(N次)
- 方B："滚"(N次) "烦"(N次)

### 追逐/回避信号
- 方A问"你在干嘛"：N次
- 方B说"在忙/晚点"：N次

## 🔇 沉默窗口
| 起始 | 时长 | 严重度 |
|------|------|--------|
| MM-DD HH:MM | N小时 | 正常/注意/危险 |

## ⚠️ 依恋行为标记
### 焦虑型信号（方A）
- 反复确认安全感：N次
- "算了"隐含失望：N次

### 回避型信号（方B）
- 超时回复（>2h）：N次
- 单字敷衍：N次

## 🎯 关键时刻
1. **MM-DD HH:MM** — 情绪转折："..."
2. **MM-DD HH:MM** — 沉默窗口开始

## 📋 复盘建议
1. ...
2. ...
```

### JSON 结构化数据（供复盘框架 API 调用）

```json
{
  "meta": {
    "export_time": "...",
    "contact_pair": ["方A", "方B"],
    "date_range": {"start": "...", "end": "..."}
  },
  "time_dimension": { ... },
  "emotion_dimension": { ... },
  "communication_pattern": { ... },
  "attachment_markers": { ... },
  "emotion_transitions": [
    {
      "timestamp": "...",
      "type": "positive_to_negative",
      "sender": "方A",
      "trigger_message": "...",
      "severity": 6
    }
  ],
  "warnings": [
    {
      "type": "pursuit_escape_cycle",
      "severity": "medium",
      "description": "检测到N次追逐-逃避循环"
    }
  ],
  "ready_for_retrospective": true,
  "retrospective_focus_suggestions": ["...", "..."]
}
```

---

## 关联 Skill

- `couple-retrospective` — 主框架（使用本 skill 的输出作为 Step 0 素材）
- `couple-retrospective-adversarial` — 多轮对抗（使用格式化时间线作为输入）
- `couple-attachment-analysis` — 依恋诊断（使用依恋行为标记作为输入）

**注意：** 本 skill 已内置微信解密工具，无需外部依赖。解密工具位于 `tools/` 目录。

---

## 踩坑记录

1. **Key 有效期**：Key 从微信进程内存提取，**每次微信重启后必须重新提取**。
2. **时间戳单位**：Windows 微信 CreateTime 是秒级 Unix 时间戳。若数值 > 1e12 则为毫秒，需除以 1000。
3. **Emoji 干扰**：关键词统计前需去除 Emoji，否则匹配失败。
4. **长消息拆分**：相同 CreateTime 的连续消息可能是一条被拆分的长消息，需合并。
5. **只读查询**：查询数据库时使用 `mode=ro` URI，避免修改原始数据。

---

*微信聊天记录读取器 v1.0 | 2026-05-20*
